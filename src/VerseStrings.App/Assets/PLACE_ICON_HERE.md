# Tray icon

Drop your tray icon here as `icon.ico` (16x16 / 32x32 multi-resolution).

If this file is missing, the app falls back to a generic Windows information icon at runtime — the build still succeeds.
